---
interface Props {
iconLink?: string,
name: string
}
const { iconLink, name } = Astro.props
---
<span class="tech-badge glass-panel glass-relative rounded-full px-4 py-1.5 text-default text-sm font-semibold items-center gap-2 flex justify-between border-transparent" data-animate="true">
    {iconLink && (
      <img src={iconLink} title={name} alt={name} width="20" height="20" />
    )}
    <span>{name}</span>
</span>

<style>
  .tech-badge {
    opacity: 0;
    transform: translateY(8px) scale(0.98);
    filter: blur(4px);
    transition:
      opacity 500ms ease,
      transform 500ms ease,
      filter 600ms ease;
    transition-delay: var(--reveal-delay, 0ms);
    will-change: opacity, transform, filter;
  }
  .tech-badge.is-visible {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
  @media (prefers-reduced-motion: reduce) {
    .tech-badge { transition: none; transform: none; filter: none; }
  }
</style>


